# ============================================
# SadakSathi Backend — Production Dependencies
# Render-compatible (CPU-only torch via find-links)
# ============================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
starlette>=0.37.0

# --- Settings / Env ---
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0

# --- Image & Video Processing ---
opencv-python-headless>=4.9.0
Pillow>=10.2.0
numpy>=1.26.0

# --- ML / YOLO ---
# CPU-only PyTorch wheels from the official PyTorch index (avoids 2.5GB CUDA build)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu
torchvision==0.17.2+cpu

ultralytics>=8.2.0

# --- Geo (duplication detection) ---
geopy>=2.4.0

# --- NLP similarity (duplication detection) ---
# Pin python-bidi to pure-Python version (>=0.6 requires Rust/maturin)
python-bidi==0.4.2
sentence-transformers>=3.0.0
scikit-learn>=1.4.0

# --- Plate OCR (Florence-2, replaces EasyOCR) ---
einops>=0.8.0
timm>=1.0.0
sentencepiece>=0.2.0
